{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=\"$TOOL_INPUT_FILE\"; if echo \"$FILE\" | grep -qE '\\.(ts|tsx)$'; then cd \"$(git rev-parse --show-toplevel 2>/dev/null || echo .)\" && npx tsc --noEmit --pretty 2>&1 | tail -5; fi; exit 0"
          }
        ]
      }
    ]
  }
}
